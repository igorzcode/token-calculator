# Token Usage and Cost Calculator for Claude and Gemini

This is a terminal-based Python tool to estimate token usage and cost for Claude and Gemini prompts.

## Setup (PowerShell)

1. Install Python 3.10+ if not already installed.
2. Open PowerShell and navigate to this project folder.
3. Create a virtual environment (optional but recommended):
   ```powershell
   python -m venv venv
   .\venv\Scripts\Activate.ps1
